info:
  name: user by id
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/users/2
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
