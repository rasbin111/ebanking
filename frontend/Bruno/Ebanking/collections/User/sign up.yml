info:
  name: sign up
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/auth/signup
  body:
    type: json
    data: |-
      {
        "username": "rajan",
        "password": "admin",
        "firstName": "rabin",
        "lastName": "thapa",
        "email": "rajan@email.com",
        "gender": "male",
        "role": "admin"
      }
  auth:
    type: basic
    username: admin
    password: admin

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
